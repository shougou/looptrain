# LoopTrain 部署配置
# 复制此文件为 .env.deploy 并填入真实值
# .env.deploy 不会被提交到 git

# 服务器 SSH 连接
DEPLOY_REMOTE_USER=root
DEPLOY_REMOTE_HOST=your-server-ip

# 服务器路径
DEPLOY_DEVLOG_ROOT=/var/www/looptrain-devlog
DEPLOY_STANDALONE_ROOT=/var/www/looptrain-standalone
