# LoopTrain v0.6.0 完整开发计划

**日期**: 2026-06-19  
**基准**: v0.5.0 可玩 standalone + v0.6.0 Slice 0 TypeScript 骨架已完成  
**规格来源**:
- `docs/superpowers/specs/v0.6.0-narrative-state-runtime-dev-spec.zh.md` (Part I)
- `docs/superpowers/specs/runtime-architecture-design.revised.zh.md` (Part II)
- `docs/superpowers/specs/2026-06-15-companion-runtime-design.md` (Companion Runtime — NOT approved)

---

## 当前状态

### 已完成 (v0.6.0 Slice 0)

| 模块 | 内容 | 状态 |
|------|------|------|
| shared/ | ids, time, errors, result | 类型定义 |
| ids/ | RuntimeId, RuntimeIdGenerator | 实现完成 |
| engine/ | EngineResult, MemoryEventDraft, LegacyEngineAdapter | 接口定义 |
| memory/ | MemoryEvent (29类型), RuntimeClientState, SerializedMemoryState | 类型定义 |
| companion-view/ | CompanionView, Policy, VisibilityFilter, SpoilerGuard, Builder | 骨架+过滤 |
| assistant/ | Controller, Types, Classifier, PolicyEngine, Registry, Planner, LLM/Mock/Disabled, Validator, Renderer, Fallback | 骨架实现 |
| content/ | RuntimeContentLoader, ContentPathPolicy | 已加固 |
| policy/ | SpoilerPolicy, ForbiddenRevealPolicy | 完成 |
| server.js | POST /api/assistant/ask, GET /api/assistant/state | 已集成 |

编译 `tsc` 0 错误 | 测试 runtime+standalone 通过 | 5 CRITICAL 安全修复已完成

---

## Spec 层级关系

```
v0.6.0-narrative-state-runtime-dev-spec.zh.md (Part I)  ← 🔴 P0 核心
  │  Memory Runtime Core: Event Log, Knowledge, Belief,
  │  Timeline, Archive, ResetPolicy, PromptMemoryContext
  │
  ▼
runtime-architecture-design.revised.zh.md (Part II)      ← 🟡 P1
  │  CompanionView, Assistant Runtime, ActionPlanner,
  │  OutputValidator, LLMProvider, 询问助手 UI
  │  (Slice 0 骨架已完成，等 Memory Runtime 完成后对接)
  │
  ▼
2026-06-15-companion-runtime-design.md                   ← 🔵 P2 NOT APPROVED
    许知微 Companion Runtime, 解锁进程, 安全测试
    依赖 Memory Runtime + CompanionView 完成
    需要 "Approve Companion Runtime implementation" 批准
```

---

## 开发路线

### 🔴 Slice 1: Narrative State Runtime Core (Part I 核心)

**目标**: 实现 spec Part I Section 3.1 全部 — 从 flat snapshot 到分层记忆系统

**Slice 1.1 — Memory Records**
- `knowledge/Knowledge.ts` + `KnowledgeStore.ts`
- `belief/Belief.ts` + `BeliefStore.ts`
- `relationship/Relationship.ts` + `RelationshipStore.ts`
- `timeline/Timeline.ts` + `TimelineStore.ts`
- `archive/Archive.ts` + `ArchiveStore.ts`
- `profile/Profile.ts`
- `snapshot/Snapshot.ts`

**Slice 1.2 — MemoryRuntime Core**
- `storage/InMemoryMemoryStorage.ts` — 统一存储接口
- `MemoryRuntime.ts` — 核心引擎: append/query/project
- `memory/MemoryProjector.ts` — Event Log → Knowledge/Belief/Timeline
- `memory/MemoryEventDraftAppender.ts` — Draft → Event

**Slice 1.3 — Legacy Migration**
- `migration/LegacyStandaloneStateMigrator.ts` — flat snapshot → event sequence
- `migration/MigrationValidator.ts` — 一致性验证

**Slice 1.4 — Reset Layer**
- `reset/ResetPolicy.ts` — 哪些保留/重置
- `reset/ResetPlanner.ts` — 计划重置事件
- `reset/ResetApplier.ts` — 执行重置

**Slice 1.5 — Prompt Context**
- `prompt/PromptMemoryContext.ts` — LLM 可见上下文类型
- `prompt/PromptMemoryContextBuilder.ts` — 从 MemoryRuntime 构建

**Slice 1.6 — Tests**
- `tests/memory_event.test.ts`
- `tests/knowledge_store.test.ts`
- `tests/belief_store.test.ts`
- `tests/memory_projector.test.ts`
- `tests/legacy_migration.test.ts`
- `tests/reset_policy.test.ts`
- `tests/prompt_context.test.ts`

---

### 🟡 Slice 2: CompanionView + Assistant Integration (Part II)

**前置**: Slice 1 完成  
**目标**: Slice 0 骨架与 MemoryRuntime 对接

- `CompanionViewBuilder` 从 MemoryRuntime 读取数据
- 完整 spoiler 过滤逻辑
- `ActionPlanner` 基于实际 Knowledge/Belief 推荐
- Integration tests: MemoryRuntime → CompanionView → Assistant 全链路

---

### 🟡 Slice 3: Legacy Engine Integration

**前置**: Slice 1 完成  
**目标**: `engine.js` 通过 `LegacyEngineAdapter` 桥接 MemoryRuntime

- 实现 `LegacyEngineAdapterImpl`
- 所有 engine action 产生 MemoryEventDraft
- server.js 同时写 legacy state + MemoryRuntime

---

### 🔵 Slice 4: Browser MemoryRuntime + IndexedDB

**前置**: Slice 1-3 完成  
**目标**: 浏览器端持久化

- `IndexedDBMemoryStorage`
- browser `app.js` 集成
- localStorage → IndexedDB 迁移

---

### 🔵 v0.7 Companion Runtime (许知微)

**前置**: MemoryRuntime + CompanionView 完成 + 明确批准  
状态: **NOT APPROVED** — 参看 companion-runtime-design.md Section 3

---

## Slice 1 验收标准

1. `npm run build:runtime` 编译成功
2. `npm run test:runtime` 全部新增测试通过
3. `npm run test:standalone` 原有测试继续通过
4. MemoryRuntime 接收 Draft → append Event
5. Event Log 投影出 Knowledge / Belief / Timeline / Archive
6. Legacy state 正确迁移
7. Reset 保留 carryover，重置 loop-level
8. PromptMemoryContext 不泄露 hidden truth
