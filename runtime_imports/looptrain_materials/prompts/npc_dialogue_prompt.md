# NPC Dialogue Prompt

根据当前 NPC 角色卡、玩家输入、当前状态和允许释放的信息，生成 NPC 回复。

注意：

- 如果 `allowed_clues` 为空，不要透露新线索。
- 如果玩家语气温和，NPC 可以降低防备。
- 如果玩家逼问、威胁，NPC 可以恐惧、沉默或回避。
- 对话文本中可以暗示，但不能越权确认未解锁真相。

输出普通中文文本，不输出 JSON。
